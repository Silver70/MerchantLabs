scalar DateTime
scalar Decimal
scalar UUID

enum Role {
  ADMIN
  STAFF
}

enum DiscountType {
  PERCENTAGE
  FIXED
}

enum DiscountAppliesToType {
  ORDER
  PRODUCT
  CATEGORY
}

enum MediaType {
  IMAGE
  VIDEO
  FILE
}

enum AddressType {
  SHIPPING
  BILLING
}

enum OrderStatus {
  PENDING
  PAID
  SHIPPED
  CANCELLED
}

interface Error {
  message: String!
  code: String!
  details: String
}

type ErrorResponse implements Error {
  message: String!
  code: String!
  details: String
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
  totalCount: Int!
}

type Query {
  _empty: String
}

type Mutation {
  _empty: String
}
